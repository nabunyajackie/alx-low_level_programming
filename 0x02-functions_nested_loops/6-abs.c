#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: an integer
 * Description: absoute number
 * Return: integer
 */
int _abs(int n)
{
	int x = -1;
	int res;

	if (n < 0)
	{
		res = x * n;
	}
	else
	{
		res = n;
	}
	return (res);
}
